import { BrandingManager } from '@/components/admin/BrandingManager';

export const dynamic = 'force-dynamic';

/** 管理画面: ブランド設定 (issue #88)。 */
export default function AdminBrandingPage() {
  return <BrandingManager />;
}
