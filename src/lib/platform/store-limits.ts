/**
 * platform 系ストア共通の一覧上限（#274 inc1）。
 *
 * 障害・お知らせ・メンテナンス・アップデート状況・機能フラグは、時間・テナント数に比例して
 * 蓄積し得るため list() の上限を明示する。超過分は Collection.list() が warn つきで切り詰める
 * （サイレント欠落はしない）。この上限へ近づいたら、アーカイブ済みレコードの TTL 化や
 * 境界付きクエリ（GSI、#284 と統合設計）へ移行する。
 */
export const PLATFORM_LIST_LIMIT = 500;
