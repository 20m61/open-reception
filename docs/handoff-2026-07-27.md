# セッション・ハンドオフ（2026-07-27 / 統合再設計プログラム #418 Wave 0〜2 + 検証基盤の是正）

対象セッション: 第 16〜23 wave（PR #430 / #431 / #432 / #433 / #434 / #435 / #436 / #437 / #438）。
起点 `e8fde50` → 到達 `75479ae`。ユニット 3734 → **3925 件**、e2e **172/172 green**。

---

## 0. 次に何をするか（結論から）

**#422 KioskFlow 分割**。ただし一度に全部やらず、次の順で切る。

1. `useEffectiveConfiguration` フックを追加し、**feature flag 配下で**設定取得 7 箇所を
   `/api/configuration/effective` の 1 回取得へ置換する（**ファイル分割はまだしない**）
2. 実 e2e で受付フロー・VRT・a11y の回帰を確認（`--pr --e2e`。**このコンテナで動く**）
3. その後にコンポーネント分割

`src/components/kiosk/KioskFlow.tsx` は **3196 行**、設定取得は 7 箇所に分散している
（`directory` 578 / `voice` 596 / `assets` 650 / `branding` 671 / `motions` 689 /
`flow` 707 / `signage` 729 行目付近。行番号は 2026-07-27 時点）。

切替が入ると **#419 の `kiosk-dev` 除去・#420 の端末側の版報告・移行台帳 §9 の B-03〜B-05 が
まとめて解禁される**。逆に言うと、それらは #422 の前には進められない。

### 着手前に必ず通すもの

- `/issue-ac-mapping`（CLAUDE.md 規約。本書の記述も仮説として扱う）
- `docs/product-integration-plan.md`（移行台帳。**移行状態の正はこちら**）

---

## 1. 完了（main にマージ済み）

| wave | 内容 | PR |
| --- | --- | --- |
| 16 | #425 Wave 0 移行台帳 + ADR index / #419 契約（`src/domain/product-context/`） | #430（**#425 クローズ**）|
| 17 | #420 版ライフサイクルの純ロジック（`src/domain/experience-version/`） | #431 |
| 18 | #419 実配線（`src/lib/product-context/` + `GET /api/configuration/effective`） | #432 |
| 19 | #420 永続化 + **スナップショット公開** + 管理 API + 監査 | #433 |
| 20 | #420 heartbeat 報告 + 反映状況 API | #434 |
| 21 | #420 管理画面 `/admin/experience-versions` | #435 |
| 22 | e2e 実行環境の是正（**「実行不可」は誤りだった**） | #436 |
| 23 | e2e フルスイート安定化 172/172 + VRT baseline 固定 | #437 / #438 |

**#418 の Wave 0（#425）・Wave 1（#419）・Wave 2（#420）が実装として閉じた。**
#419 と #420 は**オープン継続**（残作業は §3）。

---

## 2. このセッションで確定した事実（再調査不要）

### ブラウザ e2e はこのコンテナで動く

**第 15 wave の申し送り「exit 144 でブロックされ実行不可」は誤りだった。** 真因は Playwright
1.61.1 が要求する chromium ビルド（1228）とプリインストール版（1194）の不一致で、
`executablePath` を渡せば動く。`playwright.config.ts` が `/opt/pw-browsers/chromium` を
自動検出するようにしたので、**追加の env 指定なしで `--e2e` が通る**。

この stale な記録が 5 周にわたって「UI 変更はブラウザ検証できない」という誤った前提を作り、
#422 の着手判断を歪めていた。**環境の制約を記録するときは、次の周回が再検証できる形
（再現コマンドと実際の失敗メッセージ）で残すこと。**

### e2e の project 構成は 3 段（前後で共有状態を挟む）

```
pristine-state（既定シード前提）→ chromium-ipad（本体）→ flow-mutation（破壊的）
```

- `pristine-state` = `kiosk-checkout-i18n` / `kiosk-vrt-a11y`。**在館者ゼロ・seed の担当者のみ**を
  前提にしており、他 spec が残した来訪者・担当者があると空状態の文言と画面の高さが変わって落ちる。
- `flow-mutation` = 既存（#248/#321）。フローや共有シングルトン設定を書き換える spec。

### VRT baseline の落とし穴（実際に踏んだ）

スナップショット名は既定で **project 名を含む**。project を分けただけで別名になり、
**レビュー済み baseline が孤立して、その場の描画が新 baseline として自動生成される**
（＝退行がそのまま「正」として焼き付く）。`snapshotPathTemplate` で `chromium-ipad` 名に
固定して回避した。**VRT を別 project へ移すときは baseline の解決先を必ず確認すること。**

### 端末一覧はページネーションされる

`/admin/devices` の行を e2e で掴むときは、**一意名で `device-filter-keyword` を絞ってから**。
フルスイートでは他 spec の作った端末に押し出されて 2 ページ目以降へ移る。

### 指紋が 2 種類ある（取り違え注意）

| 関数 | 含むもの | 用途 |
| --- | --- | --- |
| `computeConfigHash` | context（端末 ID）+ version + 内容 | `/api/configuration/effective` 応答の `configHash` |
| `computeSectionsHash` | **内容のみ** | 版のスナップショット指紋・版比較・端末が heartbeat で報告する値 |

端末ごとに変わる前者を「期待値」に使えないため、反映状況の突き合わせは後者で行う。

---

## 3. 各成果物の引き継ぎ事項

### A. #419 ProductContext / EffectiveKioskConfiguration — **オープン継続**

`src/domain/product-context/`（契約）+ `src/lib/product-context/`（実配線）+
`GET /api/configuration/effective`。

**残作業**:
- `/kiosk` クライアントの新経路切替（= #422 の中核）
- `kiosk-dev` を権威判断経路から除去（台帳 §6）
- **グローバルストアのテナント対応** — branding / directory / voice / motions / avatar /
  languages はテナント次元を持たない。現状は**既定テナント以外へ fail-closed**で「配らない」側に
  倒してあるが、**マルチテナント運用を始める前に解消が必須**。永続キーの変更 = スキーマ破壊なので
  **要ユーザー確認**。
- 旧個別 API の撤去（台帳 §9 B-03。#422 の切替と観測の後）

**設計上の要点**（作り直さないこと）:
- 端末実行はセッション束縛が権威。query の tenant/site/kiosk は**無視して記録**（拒否しない =
  可用性維持）。draft は端末へ配信しない。越境は 403。
- 端末束縛の解決は **fail-closed**（`device-binding.ts`）。既存の `kiosk-gate.ts` /
  `maintenance-gate.ts` は fail-open で、**同じ解決が 3 実装ある**（台帳 §5 に重複概念として登録済み）。
- 秘匿値・PII の混入は resolver が**実行時に検査して fail-closed で拒否**する
  （`payload-contract.ts`）。規約ではなく機構で担保しているので緩めないこと。
- プレビュー経路では **kioskId が当該拠点の端末か台帳で確認**する（純関数では担保できない義務。
  塞がないと他テナントの機能フラグ値が観測できる。実際に一度穴を開けて塞いだ）。

### B. #420 受付体験の版管理 — **オープン継続**

`src/domain/experience-version/` + `src/lib/experience-version/` +
`/api/admin/experience-versions`（+ `/deployments`）+ `/admin/experience-versions`。

**increment 1 の設計は途中で覆した。** 「版は構成の指紋だけを持つ」設計では、指紋が指す中身
（可変な設定ストア）が動いてしまい **AC「下書き変更が本番へ即時反映されない」を満たせない**。
`ExperienceConfigurationSnapshot` を導入し、**下書き保存時にセクション値ごと固定**、公開版は
スナップショットから配る形にした。ロールバックも中身ごと戻る。

**残作業**:
- 実検証チェッカ（asset URL / motion mapping / call route 到達性）。現在の検証は
  「秘匿値混入 = error / セクション欠落 = warning」の 2 つだけ
- **端末側の報告送信**（heartbeat にパラメータを付ける）= #422 の切替時。それまで全端末は
  `pending` として出る
- ナビ配線（`navigation.ts` は #421 が扱う）
- デモ公開モデル（#363 `domain/demo-studio/publication.ts`）との統合。台帳 §5 のとおり
  **版モデルは `domain/experience-version/` へ一本化し、両方を恒久的に残さない**

**運用が変わる点（台帳 §9 B-06 に登録済み・要ユーザー確認）**: 版管理を有効化した拠点では
「保存＝反映」ではなくなる。**版を作るまでは発生しない**ため既存拠点への即時影響はなし。
版を作っていない拠点は従来どおり live 配信で挙動不変。

### C. 移行台帳 `docs/product-integration-plan.md`（#425・クローズ済）

**移行状態の追跡はこの文書が正**（loop-queue は着手順と依存 DAG のみ持つ）。
各 PR は**自分が動かした行を同じ PR で更新する**のが自己ルール。

台帳で判明した最重要の事実: **端末の構成取得 8 経路がスコープ解決を 4 通りでやっていた**
（認証なし / query 直読み / セッション任意 / `resolveDefaultScope()` 固定）。これが #419 の存在理由。

KPI baseline は**数値を仮置きしていない**。dev に受付実績が無く本番未稼働のため。
偽の baseline は「悪化していない」の誤判定を生む。実測は #65 実機 UAT 以降。

---

## 4. ユーザー判断待ち（本ループでは進めない）

| 件 | 内容 | 現状の推奨 |
| --- | --- | --- |
| deploy 系 | #405 Inc2 Secrets Manager / #366 Stack（**月 $14.2**。現状の AWS 実績は月 $0.0005） | 実利用者が付くまで**保留** |
| グローバルストアのテナント対応 | 永続キー変更 = スキーマ破壊 | マルチテナント運用の予定次第。当面 1 テナントなら後回しで安全 |
| 台帳 §9 B-03〜B-05 | 個別 API 廃止 / signage の query スコープ廃止 / 端末未エンロール時 fallback の fail-closed 化 | **#422 の切替後**。いま踏み切ると互換経路を消すだけ |
| 台帳 §9 B-06 | 版管理を有効化した拠点の運用変更 | 拠点ごとの有効化タイミングを判断 |
| ユーザーの draft PR #427 / #428 | Opus 5 運用プロファイル・Experience Engineering 規約 | **マージ判断はユーザー**（本ループでは触らない） |

---

## 5. 運用の申し送り

- **UI / a11y に触る変更は `./scripts/quality-gate.sh --pr --e2e` を通してから PR を出す。**
  e2e が動くようになったので、これを既定にできる。
- gitleaks は未導入（`--secrets` は SKIP）。テストの擬似値は `TEST-` 接頭辞を維持する。
- マージは squash + 直列。ゲート green + レビュー blocking なしなら自動マージ（CLAUDE.md 手順 8）。
- **本セッションで踏んだ手順ミス**: 一度、feature ブランチへ切り替えずに `main` へコミットした
  （push 前に気づいて移送）。wave の頭でブランチを確認すること。
- squash マージ後、remote の feature ブランチは削除されず残る（git プロキシが delete を拒否）。
  次 wave は `git reset --hard origin/main` してから `--force-with-lease` で上書きする運用でよい
  （旧 tip の内容は squash 済みで main と content-identical であることを確認してから）。
